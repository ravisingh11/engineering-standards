{
  "$schema": "./policy.schema.json",
  "version": 2,
  "name": "baseline",
  "profiles": [
    "core",
    "github"
  ],
  "overrides": {
    "change": {
      "dependency-remediation": "not_activated"
    },
    "release": {}
  }
}
