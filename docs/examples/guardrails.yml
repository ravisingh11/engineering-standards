{
  "$schema": "../../guardrails/policy.schema.json",
  "version": 2,
  "name": "example-core-policy",
  "profiles": ["core"],
  "overrides": {
    "change": {
      "unit-tests": "enforced"
    },
    "release": {}
  }
}
